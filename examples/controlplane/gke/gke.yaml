---
apiVersion: platform.projectx.mavenwave.dev/v1alpha1
kind: ControlPlaneClaim
metadata:
  name: a-team
  namespace: a-team
spec:
  compositionSelector:
    matchLabels:
      provider: gcp
  writeConnectionSecretToRef:
    name: a-team-controlplane
  parameters:
    platform:
      version: main
    google:
      project:
        id: bradw-nextgen-devops
        number: "809429613600"
      # region: us-east
      network:
        nodes:
          cidr: 10.0.0.0/24
        pods:
          cidr: 10.1.0.0/16
        services:
          cidr: 10.2.0.0/20
      cluster:
        authorizedNetworks: {}
        identity:
          source: Secret
          secretRef:
            name: gcp-default