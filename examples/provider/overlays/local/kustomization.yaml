apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- ../../base

patchesStrategicMerge:
- provider_patches.yaml

patchesJson6902:
- target:
    group: gcp.crossplane.io
    version: v1beta1
    kind: ProviderConfig
    name: default
  patch: |-
    - op: replace
      path: /metadata/name
      value: bradw